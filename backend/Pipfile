[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.95.0"
uvicorn = ">=0.22.0"
python-multipart = ">=0.0.6"
python-dotenv = ">=1.0.0"
sqlalchemy = ">=2.0.0"
alembic = ">=1.13.0"
psycopg2-binary = ">=2.9.0"
bcrypt = ">=4.0.0"
pyjwt = ">=2.6.0"
pandas = ">=2.0.0"
numpy = ">=1.24.0"
openpyxl = ">=3.1.0"
pypdf = ">=4.0.0"
groq = ">=0.4.0"
anthropic = ">=0.105.2"
langgraph = ">=0.2.0"
sentence-transformers = ">=3.0.0"
chromadb = ">=0.4.0"
apscheduler = ">=3.10.0"

[dev-packages]

[requires]
python_version = "3.12"
